{
  "name": "lazycrate",
  "version": "1.0.1",
  "description": "Lazily serialize and deserialize variables",
  "main": "index.js",
  "scripts": {
    "test": "nyc --all --reporter=html --reporter=text mocha",
    "lint": "eslint *.js",
    "lint:fix": "eslint *.js --fix",
    "coveralls": "nyc npm test && nyc report --reporter=text-lcov | coveralls"
  },
  "repository": "github:PotatoParser/lazycrate",
  "author": "Wilson Nguyen",
  "license": "MIT",
  "precommit": "lint",
  "devDependencies": {
    "coveralls": "^3.1.1",
    "eslint": "^7.32.0",
    "mocha": "^8.3.0",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^15.1.0",
    "precommit": "^1.2.2"
  }
}
